# Spec

This folder contains the generated JSON Schema for `domain.yaml`.

Regenerate it after core changes with:

```bash
make regenerate-spec
```

VS Code workspace settings map this file to `**/domain.yaml` for YAML IntelliSense.
